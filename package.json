{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.4",
    "shelljs": "latest"
  },
  "scripts": {
    "serve": "lerna run serve --parallel --stream",
    "build": "node build.js"
  }
}
